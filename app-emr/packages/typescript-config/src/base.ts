export type Facility = {
  _id: string;
  name: string;
};

export type Profile = {
  _id: string;
  qualification: string;
  specialization: string;
};

export type User = {
  _id: string;
  name: string;
  email: string;
};
